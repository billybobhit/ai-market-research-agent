TAVILY_API_KEY=tvly-your-api-key

# Optional. If set, the agent uses OpenAI for synthesis.
# Without it, the agent still creates a structured Markdown report from Tavily results.
OPENAI_API_KEY=sk-your-openai-key
OPENAI_MODEL=gpt-4.1-mini
